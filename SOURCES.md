# Sources

Upstream inspirations for the artifacts in this plugin. Nothing in this repo is a verbatim port, every artifact has been rewritten for personal use. Credits below are courtesy and a memory aid for periodically diff-checking upstream when checking for new ideas.

## Plugin docs

| Artifact | Upstream | Notes |
|----------|----------|-------|
| `plugins/wai/WORKFLOW.md` | own creation | Canonical workflow spine the plugin ships. Documents the spine, the two implementation drivers, side paths, invariants, configuration shape (`.claude/wai.json`), subagent surface, and cadence. **2026-08-13 usage-audit rework:** the spine is no longer copied into project memory; `/setup` writes a pointer that names no commands, so editing this file cannot leave a repo stale. See `docs/adr/0004`. |
| `docs/adr/` | own creation | Architecture decision records for the plugin itself. `0001` freeform `wai-implementer`, `0002` nudge-not-block on `general-purpose`, `0003` `describe-pr` may create PRs but never pushes, `0004` spine is a pointer in `CLAUDE.local.md`. All four came out of the 2026-08-13 usage audit. |
| `CONTEXT.md` | own creation | Root glossary, definitions only. Added 2026-08-13 so `grill-me`'s docs mode is live in the repo that ships it, and so the vocabulary the re-survey introduced (pointer, context load, axis, graft, sediment) has one authoritative home. Definitional sentences moved out of `CLAUDE.md` rather than duplicated. |
| `scripts/usage-audit.sh` | own creation | Makes the 2026-08-13 usage audit repeatable: invocation counts plus disk-artifact counts, emitted dated under `reports/`. Second toolchain exception, see `docs/adr/0006`. |
| `plugins/wai/INDEX.md` | own creation | One-line-per-artifact catalog grouped by type (Skills / Agents / Commands). Linted by `scripts/check-index.sh` against the on-disk artifact tree. |
| `plugins/wai/DIFFSCAPE.md` | own, formerly the standalone diffscape plugin's README | Feature reference for the `ds` skill (browser-based code review). Documents the only engine exception in the wai plugin, Node review server, hooks, UI, vendored JS. |

## Skills

| Artifact | Upstream | Notes on the rewrite |
|----------|----------|----------------------|
| `diagnose` | [mattpocock/skills/engineering/diagnose](https://github.com/mattpocock/skills/tree/main/skills/engineering/diagnose) | Feedback-loop framing kept verbatim because it is the point. Trimmed the construction-methods list from 10 to 6. Added explicit anti-patterns section. Removed the project-specific `scripts/` reference. **Workflow-rework update:** stripped the inline fix step, output is now a Diagnosis Report only (root cause + minimal repro + proposed fix sketch). No code edits. Trivial fixes hand off to `cavecrew-builder`; non-trivial hand off to `/create-plan plans/<date>-diagnose-<slug>.md`. `Edit` removed from `allowed-tools`. Added `INVARIANT, no code here` block. |
| `setup` | own creation | One-time per-repo bootstrap for the wai workflow. Detects repo state, writes `.claude/wai.json` (context_md / adr_dir / parallel_cap), opt-in scaffolds `CONTEXT.md` + `docs/adr/0000-template.md`. Atomic temp-file rename for every write. Pairs with the `/setup` command. **2026-08-13 usage-audit rework:** tracker fields dropped from `wai.json`; the marker block moved from `CLAUDE.md` to `CLAUDE.local.md` and shrank to a pointer; `--update` now also strips legacy blocks left in `CLAUDE.md`; adds wai personal-file patterns to `.git/info/exclude`. See `docs/adr/0004`. |
| `to-spec` | [obra/superpowers/skills/brainstorming](https://github.com/obra/superpowers/tree/main/skills/brainstorming) (MIT, Jesse Vincent) + [mattpocock/skills/engineering/to-prd](https://github.com/mattpocock/skills/tree/main/skills/engineering/to-prd) | **Merged skill** replacing `brainstorming` + `to-prd`. Two interaction modes (`interview` / `synthesize`), one output target (local file `specs/<date>-<slug>.md`). Hard gate before code, mirroring brainstorming's. Includes `INVARIANT, no code here` block. Pairs with the `/to-spec` command. **2026-08-13 usage-audit rework:** `--tracker` mode cut. It had never been used once across 711 sessions, and it was the last consumer of the tracker fields in `.claude/wai.json`. |
| `grill-me` | [mattpocock/skills/grill-me](https://github.com/mattpocock/skills) + [mattpocock/skills/engineering/grill-with-docs](https://github.com/mattpocock/skills/tree/main/skills/engineering/grill-with-docs) | Merged into one skill. Always-on grilling craft (sharpen fuzzy language, stress-test scenarios, cross-reference with code, promoted from `grill-with-docs`-only into the trunk). Docs-mode behaviors (glossary challenge, `CONTEXT.md` inline updates, ADR offers) auto-engage **only** when `CONTEXT.md` (or `CONTEXT-MAP.md`) already exists, no lazy file creation. ADR offers additionally require `docs/adr/` to exist; otherwise the candidate decision lands in the session summary only. Sibling reference files `CONTEXT-FORMAT.md` and `ADR-FORMAT.md` moved from `skills/grill-with-docs/` to `skills/grill-me/`. Cross-refs in `improve-codebase-architecture` repointed accordingly. **2026-08-13 upstream re-survey:** upstream has since split this family four ways (`grill-me` / `grilling` / `grill-with-docs` / `domain-modeling`), which is two bodies published twice each against different invocation modes. Merge kept deliberately: four always-loaded pointers for two bodies buys an invocation distinction wai does not need, and the merged skill is the one that survived the usage audit. Gained one thing, a named exit to `to-questionnaire` for branches that resolve to "I need to ask someone else". |
| `cleanup-worktrees` | own creation | Original skill. Ported full SKILL.md + 3 helper scripts (`scan.sh`, `archive.sh`, `remove.sh`). Swapped absolute `~/.claude/...` paths to `${CLAUDE_PLUGIN_ROOT}/...` per repo CLAUDE.md rule. |
| `pr-triage` | own creation | Original skill. Ported full SKILL.md + `pr_triage.py` helper. Swapped absolute path to `${CLAUDE_PLUGIN_ROOT}/...`. |
| `tdd` | [mattpocock/skills/engineering/tdd](https://github.com/mattpocock/skills/tree/main/skills/engineering/tdd) | Ported from upstream. Includes 5 sibling reference docs (`tests.md`, `mocking.md`, `refactoring.md`, `deep-modules.md`, `interface-design.md`). **Workflow-rework update:** description rewritten, TDD is now auto-applied inside `/implement-plan` via the `wai-implementer` agent's invariant. Skill stays as canonical reference. Still auto-fires on explicit "use TDD" / "red-green-refactor" requests for bug-fix flows where the user wants manual control. |
| `improve-codebase-architecture` | [mattpocock/skills/engineering/improve-codebase-architecture](https://github.com/mattpocock/skills/tree/main/skills/engineering/improve-codebase-architecture) | Architecture review that produces an HTML report of deepening candidates, then grills the user on the one they pick. Ported full from upstream, SKILL.md plus 4 supporting docs (`DEEPENING.md`, `HTML-REPORT.md`, `INTERFACE-DESIGN.md`, `LANGUAGE.md`). Cross-refs into sibling `grill-me/` (`../grill-me/CONTEXT-FORMAT.md`, `../grill-me/ADR-FORMAT.md`), repointed after the `grill-with-docs` → `grill-me` merge. **2026-08-13 upstream re-survey:** `LANGUAGE.md` and `INTERFACE-DESIGN.md` removed, both now in `codebase-design`; `HTML-REPORT.md` and `SKILL.md` repointed there. |
| `handoff` | [mattpocock/skills/productivity/handoff](https://github.com/mattpocock/skills/tree/main/skills/productivity/handoff) + [humanlayer/.claude/commands/create_handoff.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/create_handoff.md) | Merged mattpocock's rules (temp-dir save, "suggested skills" section, no-duplication, redact-secrets, argument-hint) with humanlayer's section template (Task / Critical references / Recent changes / Learnings / Artifacts / Action items) and YAML frontmatter (date, git_commit, branch, repo, topic, tags). Default location: `${TMPDIR:-/tmp}/handoffs/<YYYY-MM-DD>_<HH-MM-SS>_<kebab>.md`. Filename format from humanlayer with the `ENG-XXXX` ticket bit dropped. Pairs with the ported `/resume-handoff` command. **2026-08-13 upstream re-survey:** absorbed the join from upstream's `in-progress/claude-handoff`. wai shipped both ends of a handoff and neither end of the join, so the skill now offers `claude --bg "/resume-handoff <abs path>"` and runs it when the user asks to hand the work off rather than park it. `--bg` verified present in Claude Code 2.1.231 before porting; the manual path stays the default. |
| `writing-for-agents` | [mattpocock/skills/productivity/writing-for-agents](https://github.com/mattpocock/skills/tree/main/skills/productivity/writing-for-agents) + [obra/superpowers/skills/writing-skills](https://github.com/obra/superpowers) + [anthropic/skill-creator](https://github.com/anthropics/skills/tree/main/plugins/skill-creator) | **2026-08-13 upstream re-survey: replaces `write-a-skill`.** Upstream deleted `write-a-skill` and shipped `writing-for-agents` in its place, a standards doc covering every document an agent consumes (skills, agents, commands, `CLAUDE.md`, pointer-reached reference) rather than a procedure for authoring one. Ported the substance: context pointers, the two loads (context vs cognitive), the information-hierarchy ladder, completion criteria graded on clarity and demand, leading words and negation, and the pruning discipline (single source of truth, environment-as-source-of-truth, relevance, sediment, no-op hunting). Three things grafted from the old body because upstream does not carry them: the capture-intent-from-conversation opener, obra's description trap (a description that summarizes the workflow gets followed instead of the body), and the progressive-disclosure loading table, folded into the hierarchy ladder. `SKILL-MECHANICS.md` ported as the disclosed sibling and additionally holds the wai frontmatter convention, pushy descriptions, TDD-for-skills, the layout/scripts rules, and the flowchart rule, all of which were in `write-a-skill` and had nowhere else to go. |
| `codebase-design` | [mattpocock/skills/engineering/codebase-design](https://github.com/mattpocock/skills/tree/main/skills/engineering/codebase-design) | **2026-08-13 upstream re-survey: a collapse, not new capability.** wai carried the deep-module vocabulary in two places and the interface-design material in two more (`tdd/deep-modules.md`, `tdd/interface-design.md`, `improve-codebase-architecture/LANGUAGE.md`, `improve-codebase-architecture/INTERFACE-DESIGN.md`): four files, two meanings. Upstream had already resolved the same collision by extracting a shared skill. This skill is now the single home; `tdd` and `improve-codebase-architecture` point at it and the four duplicates are deleted. `DEEPENING.md` moved here from `improve-codebase-architecture/`; `DESIGN-IT-TWICE.md` is the old `INTERFACE-DESIGN.md`, renamed to match its Ousterhout source. Glossary body is wai's existing `LANGUAGE.md` text, which was already a rewrite. |
| `resolving-merge-conflicts` | [mattpocock/skills/engineering/resolving-merge-conflicts](https://github.com/mattpocock/skills/tree/main/skills/engineering/resolving-merge-conflicts) | **2026-08-13 upstream re-survey.** Kept both load-bearing rules: resolve by intent traced to each side's primary source (the hunk is the symptom, the intent lives upstream of it), and always finish, never abort. Expanded upstream's 5 numbered steps with the rebase ours/theirs inversion, a concrete primary-source ladder (`git log -1 --format=%B`, `git log -S`, PR/issue, then local `specs/` and `plans/`), and a discover-checks-from-the-environment step instead of upstream's assumed typecheck/test/format. Tracker plumbing dropped. `git merge/rebase/cherry-pick --abort` added to `git-guardrails-claude-code`'s blocklist, so the invariant is enforced rather than merely stated. |
| `wizard` | [mattpocock/skills/engineering/wizard](https://github.com/mattpocock/skills/tree/main/skills/engineering/wizard) | **2026-08-13 upstream re-survey.** Generates a resumable bash wizard for steps only a human can perform. Ported as the reusable form of a boundary wai already applies case by case (`describe-pr` stops before push, `diagnose` before the fix, `local-review` before the verdict). Added the three-reason gate for when it fits (secret handling, out-of-band UI, judgment at the moment of action), and hard requirements on the generated script: resumable via a state file, verify after each human step, never echo secrets, idempotent where possible, irreversible steps flagged before the prompt. The skill never runs what it generates. |
| `wait-what` | [mattpocock/skills/productivity/wait-what](https://github.com/mattpocock/skills/tree/main/skills/productivity/wait-what) | **2026-08-13 upstream re-survey.** Re-pitch a message that did not land. Ported because the default response to "I don't follow" is a restatement, which fails for the same reason the first attempt did. Added a four-way diagnosis of the miss (missing premise, wrong altitude, unearned jargon, buried subject) so the re-pitch targets a cause instead of rephrasing, plus a no-repeated-sentences rule. Kept upstream's use of `CONTEXT.md` vocabulary. Ships with an expectation attached: if it returns restatements in practice it is a no-op and it goes, settled by running it rather than by argument. |
| `to-questionnaire` | [mattpocock/skills/productivity/to-questionnaire](https://github.com/mattpocock/skills/tree/main/skills/productivity/to-questionnaire) | **2026-08-13 upstream re-survey.** Turn a decision the user cannot answer alone into a Markdown questionnaire for the person who can. Kept upstream's core move, grill the send and not the subject, since the user can always answer who it goes to and what they need back. Wired as a named exit from `grill-me`: a branch that resolves to "I need to ask someone" is offered a questionnaire instead of being parked in the session summary. Output moved to `questionnaires/<YYYY-MM-DD>-<slug>.md` to match wai's dated-file convention. |
| `teach` | [mattpocock/skills/productivity/teach](https://github.com/mattpocock/skills/tree/main/skills/productivity/teach) | **2026-08-13 upstream re-survey.** Multi-session teaching over a stateful workspace. Ported for spec learning (ISO 15118, OCPP), where the subject is large and returned to across weeks. Two deliberate changes: state lives at `~/.claude/teach/<subject>/` rather than the current directory, since understanding of a protocol is not a property of whichever repo was open when the learning started; and the SKILL.md opens by naming when the skill does *not* apply (a bounded question is answered in context, not filed), because `research` was deliberately left unported on the grounds that findings-on-disk go unread, and without that line the two overlap and the model picks between them by coin flip. Added primary-source grounding, retrieval checks over recognition checks, and completion criteria per topic. |
| `git-guardrails-claude-code` | [mattpocock/skills/misc/git-guardrails-claude-code](https://github.com/mattpocock/skills/tree/main/skills/misc/git-guardrails-claude-code) | Installs a Claude Code hook that blocks destructive git commands. Ported full from upstream including `scripts/block-dangerous-git.sh`. User-facing `~/.claude/` references kept (they describe the install target, not the plugin layout). **2026-08-13 upstream re-survey:** blocklist gained `git merge --abort`, `git rebase --abort`, and `git cherry-pick --abort`, so `resolving-merge-conflicts`'s always-resolve invariant is enforced rather than merely stated. |
| `caveman` | [JuliusBrussee/caveman/skills/caveman](https://github.com/JuliusBrussee/caveman/tree/main/skills/caveman) (MIT, Julius Brussee) | Caveman mode with intensity levels (lite/full/ultra). Vendored SKILL.md from upstream with attribution. Dropped upstream's `wenyan-*` classical-Chinese variants, kept only the English compression levels. Skill body now hosts the cross-surface map (this skill + `cavecrew-builder` + `code-reviewer`); the standalone `CAVEMAN.md` was deleted as redundant once the skill became self-contained. |
| `ds` (Diffscape) | own, formerly [martinlitre/diffscape](https://github.com/martinlitre/diffscape) (now merged into wai) | Browser-based code review for the working tree. Imported the full plugin into wai at user request: SKILL.md, 3 hooks (`session-start.sh`, `check-review.js`, `suggest-review.js`), 2 scripts (`start-server.sh`, `stop-server.sh`), a Node review server (`server/index.js`), single-page review UI (`ui/`), and vendored deps (`diff2html`, `highlight.js`). README preserved as [`plugins/wai/DIFFSCAPE.md`](plugins/wai/DIFFSCAPE.md). **This is the wai exception to the markdown-only rule**, diffscape brings a Node engine, vendored JS, and shell scripts. The exception is explicit; do not generalize it to other plugins. |
| `writing-native-hooks` | own creation (inspired by [anthropic/hookify](https://github.com/anthropics/skills/tree/main/plugins/hookify) reference impl + Claude Code official hook docs) | Teaches Claude Code's native `settings.json` hook format directly, no engine required. Covers all 8 events (`PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `Stop`, `SubagentStop`, `SessionStart`, `Notification`, `PreCompact`), stdin/stdout JSON shape, exit-code semantics, matchers, and the common patterns (block dangerous command, lint on edit, stop-checklist, inject context). Added so wai users can author hooks without needing the hookify plugin installed. Pairs with `git-guardrails-claude-code`, which applies the same `PreToolUse` shape to destructive git commands, and with the plugin's own `general-purpose` nudge hook. |
| `using-subagents` | [obra/superpowers/skills/dispatching-parallel-agents](https://github.com/obra/superpowers/tree/main/skills/dispatching-parallel-agents) + [obra/superpowers/skills/subagent-driven-development](https://github.com/obra/superpowers/tree/main/skills/subagent-driven-development) (MIT, Jesse Vincent) | Foundational primer. Holds the universal "why subagents" framing (isolated context, crafted instructions, no inherited history), the prompt-craft rules (focused / self-contained / specific output), the canonical failing-tests prompt example, the common-mistakes table, the model-selection rubric, and verification basics. **Workflow-rework update:** description trimmed to point downstream artifacts at this primer. **2026-08-13 usage-audit rework:** downstream list is now `/implement-plan` (DAG walker), `/fix-findings` (flat-list walker), and `/create-plan`; the other three callers were cut. |
| `systematic-debugging` (merged into `diagnose`) | [obra/superpowers/skills/systematic-debugging](https://github.com/obra/superpowers/tree/main/skills/systematic-debugging) (MIT, Jesse Vincent) | Not a separate skill, merged unique ideas into existing `diagnose`: multi-layer evidence-gathering at component boundaries (Phase 5), "3+ failed fixes → question the architecture" rule (Phase 6), rationalizations table (anti-patterns). Existing `diagnose` keeps its feedback-loop-first framing. |
| `test-driven-development` (merged into `tdd`) | [obra/superpowers/skills/test-driven-development](https://github.com/obra/superpowers/tree/main/skills/test-driven-development) (MIT, Jesse Vincent) | Not a separate skill, merged unique ideas into existing `tdd`: Iron Law + delete-and-restart rule, mandatory verify-fail / verify-pass gates as named workflow steps, rationalizations table. Existing `tdd` keeps mattpocock's vertical-slice / tracer-bullet framing. **2026-08-13 upstream re-survey:** `deep-modules.md` and `interface-design.md` deleted, their content now lives once in `codebase-design`, which the pre-code checklist points at. |
| `writing-plans` (merged into `/create-plan`) | [obra/superpowers/skills/writing-plans](https://github.com/obra/superpowers/tree/main/skills/writing-plans) (MIT, Jesse Vincent) | Not a separate skill, added a "bite-sized checkbox format" branch to the `/create-plan` command for subagent-driven execution. Steps are 2-5 minute actions, each `- [ ]` checkbox. Includes the no-placeholders rules + type-consistency self-review. Phased format remains the default. |
| `writing-skills` (merged into `write-a-skill`) | [obra/superpowers/skills/writing-skills](https://github.com/obra/superpowers/tree/main/skills/writing-skills) (MIT, Jesse Vincent) | Not a separate skill, added "description trap" (don't summarize the workflow in the description; Claude follows the summary and skips the body), TDD-for-skills (rationalizations are the failing test), and flowchart-only-for-non-obvious-decisions sections to existing `write-a-skill`. Dropped the 4 supporting docs (`anthropic-best-practices.md`, `persuasion-principles.md`, `testing-skills-with-subagents.md`, `graphviz-conventions.dot`) and `render-graphs.js`, kept inline only what was substantively new. |
| `create-standards-checker` | own, generalised from local `~/.claude/agents/standards-compliance-checker.md` + the now-killed `wai-ev` plugin's `ev-standards-compliance-checker` agent | **Factory skill.** Generates a domain-specialized `<domain>-standards-compliance-checker` agent into `.claude/agents/` of the current repo, plus seeds `.claude/compliance-specs.json` with the chosen alias → spec-path entries. Interactive inputs: domain slug, auto-trigger phrases, alias/path pairs, default code globs, edition gotchas. Generated agents are self-contained, inline the full method (preflight → extract normatives → grep → classify ✓/⚠/✗/-) so they don't depend on this skill at runtime. Re-run the skill to regenerate when the template evolves; the user owns the re-generate decision. Atomic writes only; never silently overwrites existing agents. Replaces the killed `wai-ev` plugin, domain specialisation is now per-repo (any domain), not a separate marketplace plugin (EV-only). Template body in sibling file `agent-template.md`. Pairs with the `/spec-registry` command for ongoing registry maintenance. |
| `commit` | [humanlayer/.claude/commands/commit.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/commit.md) + [humanlayer/.claude/commands/ci_commit.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/ci_commit.md) + [JuliusBrussee/caveman/skills/caveman-commit](https://github.com/JuliusBrussee/caveman/tree/main/skills/caveman-commit) (MIT, Julius Brussee) | Three-way merge into a single skill. Converted humanlayer commands → skill so it auto-fires on "commit"/"save my work" intent. Absorbed `caveman-commit`'s full message-format ruleset as always-on default, Conventional Commits (`<type>(<scope>): <summary>`), ≤50 char subject, banned phrases (no AI attribution, no "I/we/now/currently", no emoji), body-required cases (breaking changes, security fixes, data migrations, reverts), and the two upstream examples. Mode selection: explicit `--ci` / `--yes` / `--interactive` / `--message-only` arg wins; otherwise auto-detect via `$CI` or absence of TTY; default interactive. `--message-only` outputs the formatted message in a code block without staging/committing (preserves caveman-commit's standalone use case). Kept the stronger "exclusions" list from `ci_commit` (scratch, `.env`, `dist/`, generated code). "No `git add -A`" + "no Claude attribution" hard rules preserved, both load-bearing. |

## Commands

| Artifact | Upstream | Notes on the rewrite |
|----------|----------|----------------------|
| `setup` | own creation | Explicit invocation surface for the `setup` skill. Pass-through for `--update`. Invokes the skill, which handles state detection, prompting, atomic `.claude/wai.json` write, and the marker-comment injection in `CLAUDE.md`. |
| `to-spec` | own creation | Explicit invocation surface for the `to-spec` skill. Flags pass through (`--interview` / `--synthesize` / `--tracker`). |
| `diagnose` | own creation | Explicit invocation surface for the `diagnose` skill. Default mode takes a bug description; `--from-ci <pr-number-or-url>` fetches failing GitHub Actions logs via `gh run view --log-failed`, extracts failures, runs the same diagnose loop. Output is a Diagnosis Report at `plans/<date>-diagnose-<slug>.md`. No code edits. |
| `resume-handoff` | [humanlayer/.claude/commands/resume_handoff.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/resume_handoff.md) | De-humanlayered. Dropped ENG-XXXX ticket-number lookup, `humanlayer thoughts sync`, `thoughts/shared/{plans,research,handoffs}` path conventions. Accepts any handoff file path. Trimmed from 217 lines to ~80; kept the read-verify-plan-go flow which is the substance. Added explicit "verify before trusting" step because the original assumed it but didn't demand it. |
| `local-review` | [humanlayer/.claude/commands/local_review.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/local_review.md) | De-humanlayered + generalised. Auto-discovers upstream repo name from `origin`, places worktree under `.claude/worktrees/review-<short>`, names the local branch `review/<username>/<branch>`. **Workflow-rework update, V2 chain:** final step suggests a `code-reviewer` dispatch → `/ds <base-commit>` → manual `gh pr review` for the verdict. No more auto-`gh pr review`. Notes that the review worktree is auto-cleaned by `cleanup-worktrees` once the upstream PR merges. |
| `describe-pr` | [humanlayer/.claude/commands/describe_pr.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/describe_pr.md), [humanlayer/.claude/commands/describe_pr_nt.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/describe_pr_nt.md), [humanlayer/.claude/commands/ci_describe_pr.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/ci_describe_pr.md) | Merged the three variants into one. **Workflow-rework update, file-only:** output target moved from `/tmp/<repo>/prs/<num>_description.md` to `.claude/pr-descriptions/<branch-slug>.md` (lives in the repo so it survives reboots). Removed all `gh pr edit` calls and the push step. Auto-detects `.github/PULL_REQUEST_TEMPLATE.md`. Includes `INVARIANT, no code here` block. **2026-08-13 usage-audit rework:** the audit found 95 `gh pr create` calls against 25 generated description files, so the file-only stop was costing ~70 PRs their description. The command now offers `gh pr create --body-file` (or `gh pr edit` when a PR exists) behind an explicit per-invocation confirmation, and stops with an instruction if the branch has no upstream. It still never runs `git push`. See `docs/adr/0003`. |
| `create-plan` | [humanlayer/.claude/commands/create_plan_nt.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/create_plan_nt.md) | Default plan location is `plans/<YYYY-MM-DD>-<kebab>.md`. **Workflow-rework update, DAG output:** plans now emit a DAG, not phases. Task heading format: `### T<n>, <title>`. Immediately under: `depends_on: [T<n>, T<m>]` plain text line. Then checkbox steps. Parse contract is documented in-band; `/implement-plan` is its only reader. Includes `INVARIANT, no code here` block. **2026-08-13 usage-audit rework:** tracker mirroring dropped, along with the `issue: #N` line in the parse contract. Research dispatch now names `codebase-analyzer` plus the built-in `Explore`, since the two deleted locator agents were skipped even when this command told you to spawn them. |
| `iterate-plan` | [humanlayer/.claude/commands/iterate_plan_nt.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/iterate_plan_nt.md) | Dropped thoughts-specific path hints. Wires to ported agents. Kept the read-confirm-edit flow and the surgical-edits-not-rewrites rule. **Workflow-rework update:** added `INVARIANT, no code here` block. |
| `implement-plan` | [humanlayer/.claude/commands/implement_plan.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/commands/implement_plan.md) | **Workflow-rework rewrite, subagent-driven default + DAG walk.** Reads the DAG plan format from `/create-plan`. Walks the DAG: per ready task → dispatch `wai-implementer` → on DONE → `wai-spec-reviewer` → on pass → `code-reviewer` (which self-dispatches narrow specialists per heuristic) → on pass mark task `[x]`. Parallel up to `parallel_cap` from `.claude/wai.json` (default 3). On any reviewer rejection: retry the implementer once with the failure trace + reviewer comments appended. Second failure → quarantine the task + propagate `blocked` to DAG descendants. End-of-walk report lists green vs blocked tasks with last failure mode + last implementer diff + last reviewer verdict per quarantine. Override agents locally by dropping same-named files in `.claude/agents/`. |
| `spec-registry` | own, generalised from the killed `wai-ev` plugin's `/ev-specs` command | CRUD CLI for `.claude/compliance-specs.json`, the registry that `<domain>-standards-compliance-checker` agents read at audit time. Subcommands: `list`, `check` (validates paths exist + PDF edition matches `edition` field via `pdfinfo` + page 1 read), `add`, `update`, `remove`, `init`, `where`. All writes go through `jq` + atomic temp-file rename + user-confirmed diff. Decoupled from any specific agent, works on raw JSON. Never invokes a compliance checker, never edits code, never touches git/network. Pairs with the `create-standards-checker` skill (skill seeds the registry at generation time; this command maintains it after). |

| `fix-findings` | own creation | **2026-08-13 usage audit, new artifact.** The audit found roughly 30 `general-purpose` dispatches following one shape: produce a numbered findings list (handoff doc, clustered review output, diagnosis report), then fan out one agent per item. `/implement-plan` does exactly that fan-out but only parses a `T<n>` / `depends_on` DAG, so a flat list had nowhere to go. Same machine, different parser: no dependency ordering, fully parallel up to `parallel_cap`, `wai-implementer` (freeform) → `wai-spec-reviewer` → `code-reviewer` per item, retry-once then quarantine. |

## Hooks

Diffscape's three hooks are documented in [`plugins/wai/DIFFSCAPE.md`](plugins/wai/DIFFSCAPE.md).

| Artifact | Upstream | Notes |
|----------|----------|-------|
| `hooks/nudge-general-purpose.sh` | own creation | **2026-08-13 usage audit, new artifact.** `PreToolUse` hook on `Task`/`Agent`. Reads `tool_input.subagent_type`; when it is `general-purpose`, emits the wai roster as a `systemMessage` and allows the call. Advisory by design, it always exits 0 and never exits 2. Fails open on missing `jq`, empty stdin, or unparseable payload, so a bad payload can never stall an agent dispatch. A hard deny was considered and rejected, see `docs/adr/0002`. |

## Agents

| Artifact | Upstream | Notes on the rewrite |
|----------|----------|----------------------|
| `codebase-analyzer` | [humanlayer/.claude/agents/codebase-analyzer.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/agents/codebase-analyzer.md) | Same voice rewrite. Cut the long "What NOT to Do" repetition. Added "no line reference = guessing" sharpener. Kept the no-critique guardrail. |
| `web-search-researcher` | [humanlayer/.claude/agents/web-search-researcher.md](https://github.com/humanlayer/humanlayer/blob/main/.claude/agents/web-search-researcher.md) | Voice rewrite. Added explicit "say I don't know" requirement after N searches, humanlayer's version implied this but the model needs the rule explicit. Kept the quote-don't-paraphrase rule because it's the actual quality lever. |
| `cavecrew-builder` | [JuliusBrussee/caveman/agents/cavecrew-builder.md](https://github.com/JuliusBrussee/caveman/tree/main/agents/cavecrew-builder.md) (MIT, Julius Brussee) | Surgical 1-2 file editor with hard refuse for 3+ file scope. Caveman-format diff receipts. Voice rewrite, added a wai-context intro paragraph explaining why the agent exists (cheap delegated edits, 1-2 file limit prevents scope drift) and restructured the refusals into a table. Body carries a one-line cross-ref to the `caveman` skill, which hosts the cross-surface map (after `CAVEMAN.md` was deleted). |
| `wai-implementer` | own, extracted from [obra/superpowers/skills/subagent-driven-development](https://github.com/obra/superpowers/tree/main/skills/subagent-driven-development) (MIT, Jesse Vincent) prompts | Implements a single DAG task from a wai plan. Receives task ID + title + depends_on + checkbox steps + scene-setting context as the prompt. TDD Iron Law baked in as invariant. Returns a structured report (Status / Summary / Files changed / Tests / Commits / Self-review). Override locally by dropping `.claude/agents/wai-implementer.md`. **2026-08-13 usage-audit rework:** gained a freeform mode. The audit found 141 `general-purpose` dispatches against 86 here, because the DAG-task-ID requirement rejected the "investigate this and fix it" shape that is most of the real work. Mode detection is trivial: a `T<n>` ID means DAG mode, anything else means freeform. Iron Law and receipt format unchanged. Dispatched by `/implement-plan`, `/fix-findings`, or directly. See `docs/adr/0001`. |
| `wai-spec-reviewer` | own, extracted from [obra/superpowers/skills/subagent-driven-development](https://github.com/obra/superpowers/tree/main/skills/subagent-driven-development) (MIT, Jesse Vincent) prompts | Reviews a change against the spec it came from, and nothing else. Pass/fail verdict + 1-2 line reason. No quality nitpicks. **2026-08-13 upstream re-survey:** gained a standalone mode. The implementer report is now optional, and its absence is the mode switch: plan mode keeps the do-not-trust-the-report framing and the retry/quarantine contract, standalone mode runs against spec plus diff for `code-reviewer`. Consolidating it into `code-reviewer` was considered and rejected, see ADR-0005. |
| `code-reviewer` | [anthropic/pr-review-toolkit/agents/code-reviewer.md](https://github.com/anthropics/skills/tree/main/plugins/pr-review-toolkit/agents/code-reviewer.md) + [JuliusBrussee/caveman/skills/caveman-review](https://github.com/JuliusBrussee/caveman/tree/main/skills/caveman-review) (MIT, Julius Brussee, compressed output mode) | General code review against `CLAUDE.md` with 0-100 confidence scoring (≥ 80 only). **Absorbed `caveman-review` skill** as a second output mode: caller asks for "caveman" / "compressed" / "terse" → one-line-per-finding compressed format with severity emoji. Body carries a one-line cross-ref to the `caveman` skill, which hosts the cross-surface map (after `CAVEMAN.md` was deleted). **2026-05-27: absorbed the prior quality-reviewer agent.** Added `Task` tool for self-dispatch of `silent-failure-hunter` / `pr-test-analyzer` / `comment-analyzer` / `type-design-analyzer` per heuristic. Output contract switched to `Verdict: pass\|fail` + Critical/Important/Minor/Strengths buckets (was verbose grouped by severity). Now serves both standalone and `/implement-plan` orchestrator contexts. **2026-08-13 usage-audit rework:** absorbed the standards from the owner's standalone `rigorous-pr-review` skill (itself after cursor/plugins `thermo-nuclear-code-quality-review`), which had been winning head-to-head 10-0 against the deleted `/review-pr` command. New `## Standard` section carrying all eight of the source's review rules (lead-with-findings, high-confidence-only with explicit uncertainty labelling, don't-block-on-perfect-information, concrete remedy per finding, redesign-only-when-the-local-fix-entrenches, style-and-idiom-only-when-it-matters, complexity thresholds as tripwires, read-only) and all eight structural standards (simplification-before-addition, ~1,000-line file tripwire, random-growth callouts, coherent boundaries, explicit models over loose shapes, canonical helpers over bespoke machinery, atomicity in multi-step orchestration, skepticism of "temporary" compatibility paths), plus the eight design questions as a `### Questions that drive the review` subsection. Archived copy of the source skill kept at `archive/rigorous-pr-review/`. This is now the single review surface. The source's test-strategy section is ported as a risk-proportional bullet in `## Standard`, and the dispatch heuristic gained a matching clause: `pr-test-analyzer` also fires when that rule flags a change as more than local and mechanical, even with zero test churn. The two compose deliberately, since the main reviewer's own findings are filtered at confidence ≥ 80 while specialist findings pass through, so routing borderline coverage concerns to the specialist keeps them from being suppressed. **Deliberately not ported:** the per-language C++/Rust/Python lenses (reduced to one match-local-conventions sentence) and the remedy-shape catalogue. **2026-08-13 upstream re-survey, two-axis rework** after [mattpocock/skills/engineering/code-review](https://github.com/mattpocock/skills/tree/main/skills/engineering/code-review). Took three pieces and left the orchestration wrapper, which is the `/review-pr` shape that already lost 10-0. (1) The twelve-smell Fowler baseline, as disclosed reference in `agents/SMELLS.md`, with upstream's two binding rules: the repo's documented standard overrides it, and every smell is a judgment call rather than a hard violation. (2) Fixed-point preflight: resolve the ref and confirm a non-empty three-dot diff before dispatching anything. (3) A standalone spec axis. The audit-era gap was that wai's spec review only existed inside the DAG walker, so every non-plan review path ran with no spec axis at all; `code-reviewer` now dispatches `wai-spec-reviewer` as a fifth specialist once it locates a spec. Verdicts are emitted per axis and never reranked, per ADR-0005. |
| `comment-analyzer` | [anthropic/pr-review-toolkit/agents/comment-analyzer.md](https://github.com/anthropics/skills/tree/main/plugins/pr-review-toolkit/agents/comment-analyzer.md) (Apache 2.0, Anthropic) | Advisory comment audit, checks accuracy against code, completeness, long-term value, misleading wording. Voice rewrite. Kept the read-only "advise, don't edit" boundary. |
| `pr-test-analyzer` | [anthropic/pr-review-toolkit/agents/pr-test-analyzer.md](https://github.com/anthropics/skills/tree/main/plugins/pr-review-toolkit/agents/pr-test-analyzer.md) (Apache 2.0, Anthropic) | Behavioral-coverage analysis with 1-10 criticality rubric. Voice rewrite. Kept the rubric (the substance) and the "tests fail when behavior changes, not implementation details" closing principle. |
| `silent-failure-hunter` | [anthropic/pr-review-toolkit/agents/silent-failure-hunter.md](https://github.com/anthropics/skills/tree/main/plugins/pr-review-toolkit/agents/silent-failure-hunter.md) (Apache 2.0, Anthropic) | Error-handling auditor with zero tolerance for silent failures, broad catches, and unjustified fallbacks. Voice rewrite. Kept the five non-negotiable rules, the per-handler scrutiny checklist, and the severity rubric. |
| `type-design-analyzer` | [anthropic/pr-review-toolkit/agents/type-design-analyzer.md](https://github.com/anthropics/skills/tree/main/plugins/pr-review-toolkit/agents/type-design-analyzer.md) (Apache 2.0, Anthropic) | Type-design review with 1-10 ratings across encapsulation, invariant expression, usefulness, enforcement. Voice rewrite. Kept the four-dimension rubric, the anti-patterns list, and the make-illegal-states-unrepresentable principle. |

## Re-survey checklist

When checking upstream for new ideas, look at:

- [https://github.com/mattpocock/skills/tree/main/skills](https://github.com/mattpocock/skills/tree/main/skills), focus on `engineering/`, `productivity/`. Check `in-progress/` for ideas that have matured. **Last re-surveyed 2026-08-13**, see the section at the end of this file for what was taken and what was deliberately left.
- [https://github.com/humanlayer/humanlayer/tree/main/.claude](https://github.com/humanlayer/humanlayer/tree/main/.claude), `commands/` and `agents/`.
- [https://github.com/obra/superpowers/tree/main/skills](https://github.com/obra/superpowers/tree/main/skills), Jesse Vincent's process-discipline skills. Several rationalization tables + Iron Laws were merged into existing wai skills (`diagnose`, `tdd`, `write-a-skill`, `/create-plan`). Worth diff-checking after major superpowers releases for new entries.
- [https://github.com/anthropics/skills/tree/main/plugins/pr-review-toolkit](https://github.com/anthropics/skills/tree/main/plugins/pr-review-toolkit), Anthropic's PR review toolkit. 6 specialized agents (`code-reviewer`, `code-simplifier`, `comment-analyzer`, `pr-test-analyzer`, `silent-failure-hunter`, `type-design-analyzer`) + `/review-pr` orchestrator. All ported to wai with voice pass. Worth diff-checking for new agents (e.g., security, perf).
- [https://github.com/anthropics/skills/tree/main/plugins/hookify](https://github.com/anthropics/skills/tree/main/plugins/hookify), Anthropic's hookify plugin. Engine (Python: `core/`, `hooks/`, `matchers/`) NOT vendored, wai is markdown-only by design. Ported `conversation-analyzer` agent (generic transcript miner, useful even without the engine) and `writing-hookify-rules` skill (DSL reference, requires the separate hookify plugin to actually run). Skipped `/hookify`, `/configure`, `/list`, `/help` commands (engine-dependent).
- [https://github.com/anthropics/skills/tree/main/plugins/security-guidance](https://github.com/anthropics/skills/tree/main/plugins/security-guidance), Anthropic's security-guidance plugin. Engine (Python `PreToolUse` hook) NOT vendored. Pattern list extracted into the `security-patterns` skill + a bash-only re-implementation of the hook. Worth diff-checking for new patterns when the plugin updates (e.g. SSRF, prototype pollution, deserialization expansions).
- [https://github.com/anthropics/skills/tree/main/plugins/claude-md-management](https://github.com/anthropics/skills/tree/main/plugins/claude-md-management), Anthropic's CLAUDE.md tooling. Fully ported and consolidated into the single `claude-md` skill (audit mode + revise mode), with all 3 reference docs vendored verbatim. No engine; pure markdown. Upstream `claude-md-improver` skill and `revise-claude-md` command merged together in wai.
- [https://github.com/anthropics/skills/tree/main/plugins/claude-code-setup](https://github.com/anthropics/skills/tree/main/plugins/claude-code-setup), Anthropic's Claude Code setup recommender. Fully ported: `claude-automation-recommender` skill + 5 reference docs vendored verbatim. SKILL.md got wai-cross-refs so recommendations don't re-suggest upstream artifacts already in wai. Worth diff-checking when Anthropic ships new official plugins / MCP servers.
- [https://github.com/anthropics/skills/tree/main/plugins/skill-creator](https://github.com/anthropics/skills/tree/main/plugins/skill-creator), Anthropic's skill creation + evaluation infrastructure. Engine (Python eval/benchmark scripts, 3 eval-orchestrator agents `analyzer` / `comparator` / `grader`) NOT vendored, wai is markdown-only and the agents are inert without the orchestrator. Unique non-engine ideas merged into `write-a-skill`: capture-intent-from-conversation, pushy descriptions for undertriggering, 3-level progressive-disclosure table, domain-organization pattern (per-variant reference files).

Add a row to the tables above whenever you port a new artifact. Update existing rows if you do a substantive rewrite from a later upstream version.

## 2026-08-13 usage audit

The plugin was measured against 711 real transcripts rather than against intent, and cut from 56 artifacts to 38. The discriminator that mattered was **"did it leave artifacts on disk"**, not "was it invoked": one-shot bootstrap skills like `setup` and `create-standards-checker` have no recent invocations by design, and `describe-pr` looked unused by slash count while having written 25 description files.

Cut for zero invocations and zero output: `claude-automation-recommender`, `claude-md`, `finishing-a-development-branch`, `issue-triage`, `prototype`, `receiving-code-review`, `requesting-code-review`, `security-patterns`, `to-issues`, `using-git-worktrees`, `verification-before-completion`, `zoom-out`, `/research-codebase`, `/review-pr`, `/validate-plan`, `codebase-locator`, `codebase-pattern-finder`, `code-simplifier`, `conversation-analyzer`.

Three findings drove the behavior changes, and they are the ones worth remembering:

1. `general-purpose` was dispatched 141 times, more than every wai agent combined. Every wai agent was too narrow to accept "investigate this and fix it". Fixed by ADR-0001 (freeform `wai-implementer`) and ADR-0002 (nudge hook).
2. The owner bypassed their own skills where the skill added a step rather than removing one: 95 raw `git worktree add`, 26 raw `git commit`, 95 `gh pr create` against 25 generated descriptions.
3. An outside skill beat a plugin one head-to-head: `rigorous-pr-review` ran 10 times, `/review-pr` zero. The winner had opinionated standards; the loser had orchestration. Standards absorbed into `code-reviewer`.

Re-run the measurement before the next strip. Invocation counts come from `"skill":"..."` and `"subagent_type":"..."` in `~/.claude/projects/**/*.jsonl`; disk artifacts are the better signal.

## 2026-08-13 upstream re-survey (mattpocock/skills)

First re-survey since the usage audit. Upstream had reorganized: `write-a-skill` and `to-prd` are gone, the grilling family split four ways, and `codebase-design` was extracted as shared vocabulary.

**Taken:** `writing-for-agents` (replacing `write-a-skill`), `codebase-design` (as a collapse of four existing duplicate files), the Fowler smell baseline and fixed-point preflight and standalone spec axis from `code-review`, `resolving-merge-conflicts`, `wizard`, `wait-what`, `to-questionnaire`, `teach`, and the background-agent join from `in-progress/claude-handoff`.

**Deliberately left, with the reason, so the next re-survey does not re-litigate them:**

| Upstream | Why not |
|----------|---------|
| `grilling`, `domain-modeling`, `grill-with-docs` | The four-way split is two bodies published twice each against different invocation modes. wai's merged `grill-me` covers both doors through one pointer. Checked the live concern directly: the owner runs `CONTEXT.md` in real repos and has not seen glossary drift between sessions, which is the only gap a model-invoked `domain-modeling` would close. |
| `triage`, `to-tickets`, `prototype` | wai's equivalents (`issue-triage`, `to-issues`, `prototype`) were cut by the usage audit for zero output. Upstream shipping them is not new evidence. |
| `implement` | `/implement-plan` is this, and it survived the audit. |
| `wayfinder` | Real gap (open decisions outliving one session) but upstream's version is native blocking links on a tracker, and every tracker path in wai was cut. Owner's call: grilling plus ADRs already cover it. |
| `ask-matt` | Router over user-invoked skills. `INDEX.md` is the human-facing router; the model-facing problem is pointer wording, which ADR-0007's audit addresses at the cause. |
| `research` | Its added value over wai's `web-search-researcher` is a findings file on disk. Owner's call: findings stay in context, since a file nobody reopens costs disk and looks alive. `teach` carries the persistence case instead, and says so in its body so the two do not overlap. |
| `setup-pre-commit` | One-shot bootstrap for a TS/JS toolchain. `code-reviewer` already defers to whatever tooling enforces, and `commit` owns the commit-time seam. |
| `setup-matt-pocock-skills` | wai has `/setup`. |
| `in-progress/writing-beats`, `writing-fragments`, `writing-shape` | Prose craft for articles, not engineering. |
| `in-progress/setup-ts-deep-modules` | dependency-cruiser wiring, TypeScript only. |
| `in-progress/loop-me` | Overlaps `grill-me` + `to-spec`, and the harness now ships a built-in `/loop`. |

Upstream's `in-progress/` bucket is explicitly allowed to change or disappear without warning, which is a bad property to inherit given there is no upstream sync here. Take ideas from it, never structure.
